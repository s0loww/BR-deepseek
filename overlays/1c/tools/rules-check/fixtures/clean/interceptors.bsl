&Вместо("ОбработкаЗаполнения")
Процедура ДЕМО_ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	ПродолжитьВызов(ДанныеЗаполнения, СтандартнаяОбработка);
	ЗаполнитьДополнительныеРеквизиты(ДанныеЗаполнения);
КонецПроцедуры

&Перед("ПриЗаписи")
Процедура ДЕМО_ПриЗаписи(Отказ)
	// оригинал выполнится сам — ПродолжитьВызов здесь не нужен
	ПроверитьПраваДоступа();
КонецПроцедуры

&После("ПриЗаписи")
Процедура ДЕМО_ПриЗаписиПосле(Отказ)
	ОбновитьСвязанныеДанные();
КонецПроцедуры

&Вместо("ПолучитьСумму")
Функция ДЕМО_ПолучитьСумму(Параметры)
	Возврат ПродолжитьВызов(Параметры) * КоэффициентПересчета();
КонецФункции
