&Перед("ПриЗаписи")
Процедура ДЕМО_ПриЗаписи(Отказ)
	ПродолжитьВызов(Отказ);
КонецПроцедуры

&Вместо("ОбработкаПроведения")
Процедура ДЕМО_ОбработкаПроведения(Отказ, Режим)
	СформироватьДвижения(Отказ);
КонецПроцедуры

&Вместо("ПриЗаписиНаСервере")
Процедура ДЕМО_ПриЗаписиНаСервере(Отказ)
#Вставка
	ДополнительнаяПроверка(Отказ);
#КонецВставки
	ПродолжитьВызов(Отказ);
КонецПроцедуры
